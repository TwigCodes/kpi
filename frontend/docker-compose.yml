version: '3'
services:
  nginx:
    container_name: nwcd-kpi
    image: registry.cn-beijing.aliyuncs.com/twigcodes/nwcdkpi-frontend-nginx
    build:
      context: .
      dockerfile: ./Dockerfile
      # args:
      #   - env=production
    volumes:
      - ./dist/browser:/usr/share/nginx/html
    #   - certs-data:/data/letsencrypt
    ports:
      - 80:80
      # - 443:443
# volumes:
#   certs:
#   certs-data:

# other compose services
